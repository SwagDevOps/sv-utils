---
loggerd:
  user:  'root'
  log_dir: '/var/log/runit/%<service>s'
  command:
    - '/sbin/chpst'
    - '-u'
    - '%<user>s'
    - '/sbin/svlogd'
    - '-tt'
    - '%<log_dir>s'
service:
  user:  'root'
  command:
    - '/sbin/chpst'
    - '-u'
    - '%<user>s:%<group>s'
    - '--'
control:
  futils: 'verbose'
  paths:
    - '/etc/sv'
    # SHOULD not be uncommented, and use SVDIR environment
    # - '/service'
